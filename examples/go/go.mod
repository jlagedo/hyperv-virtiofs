// Standalone module so this example never becomes an importable part of the
// product. The product's contract is the C ABI in ../../include/hyperv_virtiofs.h.
module example.com/hyperv-virtiofs-go-example

go 1.22
